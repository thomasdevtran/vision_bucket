# Screenshots

Add PNG/JPG screenshots of the running app here and reference them from the
top-level [`README.md`](../../README.md).

Suggested shots:

- `auth.png` — the sign-in / sign-up screen
- `home.png` — the discovery / curated shelves view
- `movie-details.png` — a movie page with reviews
- `profile.png` — a user profile with watch history and stats
- `discussion.png` — a discussion board thread

To capture them, run the app locally (`npm start`), sign in, and grab each view.
